apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitor
spec:
  project: default
  source:
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
    path: k8s/kustomize/base/monitor
  destination:
    server: {{ .Values.spec.destination.server }}
    namespace: monitor
